create table cars(id_masina int not null auto_increment primary key, 
					marca varchar(50), 
					model varchar(50),
					an_fabricatie int,
					motorizare varchar(50),
					capacitate_motor float,
					cp int,
					km float,
					culoare varchar(50),
					nr_usi int)
					
					